<config>
  <user value="{{USERNAME}}"/>
  <team value="{{TEAM}}"/>
  <passkey value="{{PASSKEY}}"/>

  <power value="{{POWER}}"/>
  <gpu value="{{GPU}}"/>
  <fold-anon value="false"/>
  <cpus v='{{CPU_COUNT}}'/>
</config>
